# Harden H01 — P0 GLOBE / RADIO

**Seat:** H01 P0 GLOBE/RADIO · Coordinator Host  
**Class:** public-suite  
**Tree:** `C:\AOS\ops\local-reason-bridge\sandbox\work\groks-eye-view-next`  
**This sitting writes:** this file only  
**Not this sitting:** `src/` edits, Playwright, KIT-06+

Lock source: KIT-00-SMOKE-LOCALHOST 2026-08-29 + FEATURE_KITS P0s + disk after KIT-01–05. Reconfirm is **disk**, not a browser run.

---

## Verdict

**GO. No BLOCK.** `GlobeCanvas` cannot unmount from civic overlay, desk, radio, or command paths on disk. Homepage is still Earth.

Fail any P0 below → stop the kit, revert civic, **keep the globe**. A future civic route or `{desk ? <Desk/> : <GlobeCanvas/>}` is **BLOCK** and is **not** on disk.

---

## 1. Disk confirm (must-read files)

### IntelApp — globe always a sibling

`src/components/intel/IntelApp.tsx` is unconditional:

```tsx
<main className="intel-shell" data-style={style}>
  <GlobeCanvas />
  <DetectionOverlay />
  <OverlayHud />
</main>
```

No flag. No `desk`. No route swap. `src/routes/index.tsx` is `/` → `<IntelApp />` only. `src/routes/` has `__root.tsx` + `index.tsx`. Auth wraps `Outlet`; it does not replace the canvas.

`GlobeCanvas` is **not** inside `OverlayHud`. Overlays sit on Earth; they do not replace Earth.

### GlobeCanvas — boot wrapper, cleanup only on unmount

`src/components/intel/GlobeCanvas.tsx`:

- Always returns `<div className="intel-globe">`.
- `useEffect(..., [])` lazy-imports `bootGlobe` (desktop) or `bootFlatMap` (phone). Cesium miss falls back to tiles **in the same node**.
- Cleanup (`cancelled = true; cleanup?.()`) runs **only** when `GlobeCanvas` unmounts.
- No pin loop. No `entities.add`. No layer subscribe.

Civic chrome cannot fire that cleanup unless `IntelApp` unmounts.

### OverlayHud footer `z-30`

`src/components/intel/OverlayHud.tsx` ~L630:

```tsx
{!clean && !chatOpen && (
  <footer className="absolute right-3 bottom-3 left-3 z-30 md:right-4 md:bottom-4 md:left-4">
```

- Header toolbar also `z-30`. Toast `z-30`. Boot splash `z-40` is a cover, not an unmount.
- Overlay panels (`RadioDeck` picker, `DeskDrawer`, `CorpusPanel`, layer rail) are `z-20`.
- Footer is hidden only for `cleanUi` or **comms open**. Radio picker does **not** eat it. Desk does **not** eat it.
- `/` focuses the command input. Submit → `runCommand`.

### RadioDeck — always mounted

Mount order at OverlayHud ~L699–702 (HUD children, outside `hud && !clean`):

```tsx
<CommsChat />
<RadioDeck />
<CorpusPanel />
<DeskDrawer />
```

`RadioDeck` (`src/components/intel/RadioDeck.tsx`):

- Function always mounts. Picker UI is gated: `return picker ? <RadioPicker/> : null`.
- `bindRadioPlayer` runs on mount (`useEffect([], …)`). Unmounting `RadioDeck` kills audio even if Play remains.
- Picker: `.radio-panel` `z-20`, `md:right-4`, **no bottom inset** (HOLE: can paint over the bar spatially). Footer `z-30` still wins hits.
- Header Play/Pause calls `useRadio.toggle()` without opening the picker.

Parser lock (`src/lib/intel/commands.ts`): radio presets + `/^(play |put on |spin )?(some )?(ccr|creedence)$/i` → `{ type: "radio", id: "ccr", on: true }` **before** corpus, desk, layers, flyTo. Station aliases (`radio.ts`): `ccr`, `creedence`, `swamp rock`. Layer on-words are only `on|enable|show|turn on|light up`. `put on` is not a layer verb. `runCommand` radio branch returns before `if (!engine)`.

### globeEngine subscribe

`src/lib/intel/globeEngine.ts` `useIntel.subscribe` (~L1355):

| Layer `.on` flip | Action |
| --- | --- |
| flights / military / vessels / satellites / earthquakes / fires / launches | seven intel loads / prunes (**KIT-00**) |
| **legislatures** | `loadLegislatures()` — `keptDesks().slice(0, 33)` billboards, prune `leg-` when off (**KIT-03**) |
| **permits** | **absent** — AHJ toggle is store + rail only |
| detection | overlay boxes, not civic pins |

`loadLegislatures` does **not** walk `_out/capitals.json` as 194 pins, harvest members, or `places-rest.ts`. Coordinates come from capitals **joined** to `kit_on_disk` atlas rows, hard-capped at 33.

Boot reload after first Earth (~L1452) still loads the **seven intel** layers only. LEG lights via the subscribe branch (share-hash `setLayer` is **after** subscribe is registered). Click `leg-*` opens kept desk; canvas stays.

Phone `flatEngine.ts`: `refreshMarks` paints the same ≤33 LEG marks when on; **no** `permits` branch. Any `.on` flip re-paints existing marks, not Rest.

---

## 2. GlobeCanvas always mounted — proof

| Claim | Disk |
| --- | --- |
| Homepage is IntelApp | `src/routes/index.tsx` |
| Canvas is an IntelApp child, always | `IntelApp.tsx` L10, no ternary |
| Overlays cannot own `.intel-globe` | OverlayHud siblings: Comms / Radio / Corpus / Desk |
| Closed desk = `DeskDrawer` `return null` | `DeskDrawer.tsx` L288–289. GlobeCanvas not in that tree |
| Boot / first-run / clean / HUD-off | Covers or hides chrome. Do not remove `GlobeCanvas` |
| Cesium `cleanup()` | GlobeCanvas unmount only (`useEffect` `[]`) |

**Unmount paths that would BLOCK (not present):**

1. New route (`/desk`, `/kept`, `/permits`) rendering DeskDrawer instead of IntelApp.
2. `{deskOpen ? <DeskDrawer /> : <GlobeCanvas />}` (or corpus / radio swap) in IntelApp.
3. Nesting Cesium inside OverlayHud so a closed panel unmounts the host div.
4. Replacing `/` with a civic page.

Dev Strict Mode remount (Vite) reboots the engine once; that is not a civic unmount. **E**

---

## 3. P0 regression risk from KIT-01–05

KIT-00 shell (globe, four first-run cards, NVG3/Normal1, Tokyo, 390px bar, Creedence, comms, FLT, seven intel) must still hold. Civic kits add surface; they must not unmount Earth or kill the tuner.

### KIT-01 — LEG/AHJ registry

**Shipped:** `LayerId` + `LAYER_META` + `LAYER_ORDER` nine rows. Store `defaultLayers()` LEG/AHJ **off**. `show legislatures` / `show permits` are **layers**, not desks.

| Risk | Why it is / is not P0 |
| --- | --- |
| Rail count includes civic when on | Cosmetic. Globe still orbits. |
| AHJ on with **zero** entities | Honest empty. Do **not** “fix” by subscribing Rest. |
| Adding `layers.permits` to globeEngine subscribe | **P0** if the branch iterates `places-rest` / 14925. Skip / revert. |

### KIT-02 — DeskDrawer

**Shipped:** `src/components/desks/DeskDrawer.tsx` mounted in OverlayHud. Store `desk` + `setDesk` mutex with `corpusOpen`. Geometry copy of CorpusPanel: `z-20`, `bottom-28`, `md:right-4 md:w-[22rem]`. Esc: comms → radio → first-run → **desk** → corpus → cockpit → track → L-rail.

| Risk | Why it is / is not P0 |
| --- | --- |
| Drawer over Earth | Intended. Canvas sibling stays mounted. |
| Footer covered at 390px | Sheet `z-20` under footer `z-30`. Raising sheet to 30/40 **is** P0-CMDBAR. Copying RadioDeck (no bottom inset) **is** P0-CMDBAR. Copying Comms (`chatOpen` hides footer) **is** P0-CMDBAR. |
| Header Radio / Comms / Layers **buttons** do not `setDesk(null)` | Keyboard G/R/L do. Opening corpus via store clears desk. **HOLE:** Radio picker can open under DeskDrawer (DeskDrawer is later in DOM, same `z-20`). Play + command bar still work. Not globe-dead. Do not “fix” by unmounting RadioDeck. |
| New toolbar letter / 6th always-visible civic button | 390px squeeze. Not unmount. Do not add. |

### KIT-03 — LEG pins

**Shipped:** `loadLegislatures` + flat marks from `keptDesks()` cap 33. Default off.

| Risk | Why it is / is not P0 |
| --- | --- |
| ≤33 delayed country billboards | Under KIT-03 budget. Not 16k. Not 194 capital dump. |
| Removing `.slice(0, 33)` or iterating `capitals.points` / members | Pin-loop P0. Revert the loop, keep Cesium. |
| Atlas click `setLayer("legislatures", on)` + `flyTo` | Subscribe adds ≤33. Canvas stays. |
| FPS death from a pin loop | P0: the layer is wrong, not the globe. Cut the loop. |

### KIT-04 — permit search (drawer, not globe)

**Shipped:** `search-lite.ts` = Core + Extra + More only. `DeskDrawer` / `runCommand` import that, **not** `places.ts`. `places.ts` still concatenates `REST_PLACES`; `catalog.ts` is the only importer and is **not** on the HUD/globe graph.

| Risk | Why it is / is not P0 |
| --- | --- |
| Vite import of `places.ts` / `catalog.ts` / `places-rest.ts` from OverlayHud, DeskDrawer, globeEngine, GlobeCanvas | Bundles 14925. **P0.** Fail closed. |
| `layers.permits` entity branch | KIT-06 later, zoom-gated, still 0 Rest at orbit. Adding it now is BLOCK for this seat’s P0 list. |
| Portal `<a href>` with “portal not verified” | Honest link. **iframe** keptglobal / hivepermitdev is P0. |

### KIT-05 — command grammar

**Shipped:** desk phrases + `find AHJ in …` / `building desk {place}` / `score the sitting in …` / `take me to the chamber in …` / `compare A and B` / `{kind} permit playbook` / `open {kept desk}` before generic flyTo. Apply arms for desk / permitSearch / keptOpen / keptCompare / permitPlaybook run **before** `if (!engine) return`, like radio/corpus.

| Risk | Why it is / is not P0 |
| --- | --- |
| Steal `put on creedence` | Radio block is first. **Must stay first.** Reorder = P0-CREEDENCE. |
| Steal `open radio` | Explicitly excluded from keptOpen remainder. |
| `show legislatures` rebound to desk | Still layer (`!/\bdesk\b/` on the LEG special-case). Rebind = kit-identity fail, not globe-dead. |
| `open tokyo` matching Japan `capitalName` | KIT-00 smoke used bare `tokyo` (still flyTo). `open tokyo` may open kept desk + fly capital height. Residual, not Creedence/globe. Do not “fix” by putting flyTo before radio. |
| Grok `fromUnknown` mapping `desk` / civic types | Radio `type === "radio"` still mapped. Do not let unknown civic eat `radio`. |

First-run four cards unchanged: Planes, please / Space junk / The planet is yelling / Just the globe. Hint now `yo tokyo · put on creedence · find AHJ in miami` (KIT-00 was `find a plane`). Residual, not a P0 fail.

---

## 4. Fail-closed list

If any row trips, **roll back the civic layer, keep the globe.** Do not finish the kit anyway.

| ID | Tripwire | Revert |
| --- | --- | --- |
| **FC-GLOBE** | `GlobeCanvas` missing from IntelApp, conditional on desk/corpus/radio, or replaced by a civic route | Delete the route/ternary. Restore `IntelApp` = Globe + Detection + OverlayHud. |
| **FC-UNMOUNT** | Opening desk / radio / corpus / LEG runs GlobeCanvas `cleanup()` / `viewer.destroy()` | Unwind the mount. Overlays `return null`; they never own `.intel-globe`. |
| **FC-RADIO-MOUNT** | `RadioDeck` not a persistent OverlayHud child; picker-gated **component** unmount | Always mount `RadioDeck`. Gate picker UI only. |
| **FC-CREEDENCE** | `put on creedence` / Play not station `ccr`; radio parse after layers/desk; scoring copy in tuner | Restore radio-before-civic parse. Tuner stays music. |
| **FC-CMDBAR** | Footer not `z-30`, hidden for desk/radio, or sheet `z ≥ 30` / `inset-0` at 390px | Sheet `z-20` + `bottom-28`. Footer mounted unless `clean` or comms. |
| **FC-16K** | `places-rest.ts` / current `places.ts` / `REST_PLACES` imported from HUD, drawer, or either engine; AHJ subscribe iterates Rest | Cut the import and the loop. AHJ stays store-only until a gated KIT-06. |
| **FC-PINS** | LEG loop walks 194 capitals or harvest members; pin loop kills FPS | Cap stays `keptDesks()` ≤33. Cut the loop. |
| **FC-IFRAME** | iframe keptglobal / hivepermitdev as “integration” | Delete iframe. Drawer + layer only. |
| **FC-NAMES** | Skill brands / invented sittings / certified-fake portals in HUD copy | Public civic wording only. LEG source “Kept harvest / public registers”; AHJ “Permit Harbor catalog (locked 2026-08-18)”. |
| **FC-HOST** | npm install, git push, `package.json` churn, write `_out/` or `kits/`, remix live gevradio | Stop. This tree stays the write target. |

**Not fail-closed (do not revert the globe for these):** empty AHJ entities while layer is on; Radio header button missing `setDesk(null)`; RadioDeck painting over the bar spatially while footer `z-30` still hits; `yo tokyo` without Grok parse; FLT source adsb.lol; cesiumStatic / icon-180 404; command hint civic addendum.

---

## 5. E / I / A

**E.** Disk 2026-08-29 harden: `IntelApp` always renders `GlobeCanvas` + `DetectionOverlay` + `OverlayHud`. `/` is IntelApp only. `GlobeCanvas` boot effect deps `[]`; no pin loop. OverlayHud footer `z-30`, hidden only if `clean` or `chatOpen`. `RadioDeck` always mounted; picker gated; `bindRadioPlayer` on mount. `put on creedence` still radio `ccr` before desk/layers. `DeskDrawer` OverlayHud sibling `z-20` `bottom-28`. Esc desk-before-corpus. `globeEngine` subscribe = seven intel + LEG `loadLegislatures` (≤33); **no** `layers.permits`. `search-lite` is Core/Extra/More. `places.ts` Rest concat is not on the HUD/globe import graph. FirstRun still four cards. LEG/AHJ default **off**. No Playwright this sitting.

**I.** KIT-01–05 can kill P0s without unmounting Cesium (Rest in the module graph, radio parse reorder, footer z-index, pin loop). The **BLOCK** condition for this seat is canvas unmount. That path is not on disk. The live HOLE is header Radio/Comms not closing desk (picker under drawer), not a dead globe.

**A.** Accept this file as the harden P0 gate for globe/radio. Fail closed: revert civic, keep GlobeCanvas / OverlayHud / RadioDeck. Do not implement KIT-06 Rest pins from this seat. Do not run Playwright. One writer: this file.

**Stop.**
