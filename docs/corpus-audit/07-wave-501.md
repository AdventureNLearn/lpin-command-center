# 07 — WAVE-501 US LINKS

Seat: 07. Target class: public-suite. File under audit: `vendor/kept/_out/501-links.json`.
Parse: PowerShell `ConvertFrom-Json` on disk (no skim, no network). Bytes on disk: **125973**. Retrieved in file: **2026-08-29**. HANDOFF.md line 16 and STATUS ACK WAVE-501 US claim the same counts.

## Counts vs HANDOFF

| Field | HANDOFF | Parse | Match |
|---|---|---|---|
| iso2 | us | `us` | yes |
| retrieved | (HANDOFF 2026-08-29) | `2026-08-29` | yes |
| nodes.length | 199 | 199 | yes |
| edges.length | 215 | 215 | yes |
| fec edges | 186 | 186 (`filing=fec`) | yes |
| usaspending edges | 29 | 29 (`filing=usaspending`) | yes |
| holes | 17 | 17 | yes |
| bytes | 125973 | 125973 | yes |

Top-level keys only: `iso2`, `retrieved`, `nodes`, `edges`, `holes`. No other graph wrapper.

Node kinds (histogram):

| kind | n |
|---|---|
| committee | 166 |
| org | 33 |

Edges have no `kind` key (0/215). Edge histograms that exist:

| filing | rel | n |
|---|---|---|
| fec | contributed | 186 |
| usaspending | granted | 29 |

AUDIT.md also claims committee 166 / org 33; same as parse.

## Graph integrity

- **JSON:** parses. All 199 nodes have `id`, `kind`, `name`, `source_url` (none empty, none null). Nodes without id: **0**. Nodes without source_url: **0**.
- **Duplicate node ids:** 0. Duplicate node names: 0. Committee `id` equals `fec_id` on all 166 committees.
- **Orphan edges:** from-missing-node **0**, to-missing-node **0**. Self-loops **0**.
- **Exact duplicate edges** (from+to+year+amount+source_url): **0**.
- **Repeat from→to pairs:** 4 pairs, all USAspending, **distinct award permalinks** (not duplicate rows). Example: DOT → Amtrak UEI `M22USH2CHRG1` is three awards (`69A36522503700AMTDC`, `69A36522503710AMTDC`, `69A36525421100FSPDC`).
- **Isolated nodes (degree 0):** 3 orgs with EINs, no incident edges: National Association Of Realtors (`36-1520690`), American Hospital Association Inc (`36-0726140`), Credit Union National Association Inc (`23-7065623`). PAC nodes for NAR/AHA/CUNA sit on the FEC side; these 990 org nodes are leftover from unretrieved Schedule I (hole H3). Isolated nodes are not orphan edges.
- **Connected components (undirected):** 15. Largest 137 (FEC PAC→candidate cluster). FEC incident nodes 166 and USAspending incident nodes 30 have **zero id overlap**. Two filing families share one file, not one graph.
- **Id formats:** FEC `C########` 166, UEI-12 23, agency slugs 7 (`department-*` 6 + `national-science-foundation` 1), EIN 3.
- **source_url hosts:** nodes — fec.gov 166, usaspending.gov 30, projects.propublica.org 3. Edges — fec.gov 186 (all the same bulk `pas224.zip`), usaspending.gov 29 (unique award permalinks).
- **member_id:** 84 committee nodes; 51 candidate committees unjoined (hole H11). Edges with member_id: 144 (122 fec + 22 usaspending). Join-only; no new sitting names in this file.
- **amounts:** all 215 have amount. FEC min **-1000** (2 refund/negative 24K rows from RNC). USAspending max ~1.56e10 (Amtrak). Zero amounts: 0.
- **program_area:** 9 USAspending edges only (infrastructure 6, law_enforcement 3). FEC 0. Matches hole H17 (dropped 18, kept 9), not the pre-block counts on hole H15.
- **issue_id:** 10 FEC edges (`israel` 5, `education` 5).
- **evidence:** every edge `Supported`.

## Sample 5 nodes

Parsed objects, not names from HANDOFF:

1. `[0]` committee `C00010603` — DNC SERVICES CORP / DEMOCRATIC NATIONAL COMMITTEE — https://www.fec.gov/data/committee/C00010603/
2. `[1]` committee `C00003418` — REPUBLICAN NATIONAL COMMITTEE — https://www.fec.gov/data/committee/C00003418/
3. `[50]` committee `C00844480` — HERTEL FOR MICHIGAN — https://www.fec.gov/data/committee/C00844480/
4. `[166]` org `36-1520690` — National Association Of Realtors — https://projects.propublica.org/nonprofits/organizations/361520690
5. `[198]` org `LGL5JFM5KDB8` — MARLBORO CITY — https://www.usaspending.gov/award/ASST_NON_15PBJA24GG04692JAGX_015

These are public committees, 501(c)(6) orgs, federal agencies, state/city recipients, and Amtrak/universities. Candidate-committee legal names carry a surname because that is the FEC Form 1 committee name, not a private-person node. FEC from-nodes are 31 party/PAC committees (DNC, RNC, DCCC, NRCC, DSCC, NRSC, AIPAC PAC, NAR PAC, unions, trade PACs). USAspending from-nodes are 7 awarding agencies. No IND person nodes. No address/email/phone/ssn/dob keys on nodes or edges. Holes H1, H2, H6, H9, H10 explicitly omit officers, treasurers, itemized persons, and forbidden Schedule B / c4–c6 donors.

## Honesty (no influence score)

Substring scan of the JSON: `influence` 0, `score` 0, `quid` 0, `pagerank` 0, `centrality` 0. No rank/score field. Co-occurrence of a PAC 24K row next to a USAspending grant is two public filings in one file, not a quid. `rel=contributed` is an FEC committee-to-committee 24K slice; `rel=granted` is an award permalink. `member_id` is a roster join, not a vote or earmark. `program_area` is a CFDA-title tag on 9 awards after Integrity dropped 18 invented tags — still not an influence score.

Hole text is sit-history, not one snapshot: H3 says `rel granted unused this sit` while 29 granted edges exist (added in H15). H13 `edges_with_program_area: 0` is the FEC committee-name rule; H15's program_area_counts are pre-block; H17 is the live tag set. Dashboard copy must use H17 + parse, not H13/H15 counts.

## Usability

A delayed dashboard **can** draw this as a small US public-filing graph:

- 199 nodes / 215 edges is loadable as two panels: FEC 24K PAC→candidate (186 edges, 2024 bulk zip) and USAspending agency→recipient awards (29 permalinks, mixed FY 2012–2025).
- Every node has a public `source_url`. Every edge has amount, year, filing, rel, source_url.
- Honest holes are in-file (17): no 8872, no 990-PF Sch B, no 990 Sch I grants, no Part VII officers, no IND persons, incomplete DCCC/NRCC/PAC tails, USAspending not a full inventory, CD 90/98/null omitted from member_id.

A delayed dashboard **must not**:

- Score, rank, or color “influence.”
- Treat the two filing families as one connected money graph (zero node overlap).
- Treat the 3 isolated 990 orgs as linked to their PACs.
- Treat two negative FEC amounts as anti-support scores.
- Treat `program_area` or `issue_id` as proof of motive.
- Claim completeness (holes H7, H8, H15).

Co-occurrence is not a quid.

## Verdict

**AUDIT PASS on counts and graph integrity.** HANDOFF nodes 199 / edges 215 (fec 186, usaspending 29) / holes 17 / 125973 bytes all parse-true. Public-entity graph. No orphan edges. No influence field.

**Not a live layer.** STATUS PARK. HANDOFF: do not merge into live gevradio. Delayed corpus only.

## E / I / A

- **E:** Parsed `iso2=us`, `retrieved=2026-08-29`, nodes 199 (committee 166, org 33), edges 215 (fec/contributed 186, usaspending/granted 29), holes 17, 0 missing ids, 0 missing source_url, 0 orphan edges, 0 influence/score keys, 3 isolated 990 orgs, 15 undirected components, FEC/USA incident overlap 0.
- **I:** A UI can list and draw these filings as a delayed, labeled public graph if it keeps FEC and USAspending separate and prints holes. It cannot infer capture, quid, or member agency from `member_id` + amount.
- **A:** HANDOFF WAVE-501 PASS is true for this file’s counts. The file is not an influence product.

## Tri-state

Claim: *501-links is a loadable US filing graph for a delayed layer.*

**yes**

Loadable JSON, internally consistent, public sources, small, no influence score. Delayed: not live-wired, not complete, two filing families plus three isolated 990 orgs. Show filings. Do not score them.
