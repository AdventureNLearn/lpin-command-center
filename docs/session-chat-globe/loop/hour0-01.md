# Hour 0 · seat 01 · insight-local.ts
Class: public-suite WIP. Not a ship. Tree groks-eye-view-next. 33 kits only. No live gevradio. No Rest. No insind.
Owns only src/lib/intel/insight-local.ts. Surgical. tsc --noEmit after edit.
Spine: Insight card, no auto-fly. askedToMove still returns null. Peek ≠ Insight.
Particle harden: comms tokens `in` and `it` are English particles, not iso2.
Bare `in`/`it` and leftover-after-strip `in`/`it` mint no card (no India/Italy steal).
`India` / `New Delhi` / `delhi` still GO kept in. `Italy` / `Rome` still GO kept it.
Iso2 `jp` / `us` / others unchanged. `ind` stays ambiguous vs Indonesia (desks.ts).
Tokyo harden: stripped `tokyo` or `tokyo japan` → keptDeskByIso("jp") at 1.2Mm.
Comms tokyo is the Japan desk (delayed NE). Command-bar tokyo stays Narita 14k — not this file.
`nrt` is still the airport preset. Do not merge tokyo into nrt on this comms path.
Jobsite harden: card only on jobsite words `\bjobsite\b`, claims desk, claim pack.
Bare locality is kept / permit / preset. `desk` / `pack` / `claims` alone do not open jobsite.
Jobsite source binds JOBSITE_HONESTY. No q. Honest hole. 0 pins.
lookupPack still empty rows. Do not invent claims. Do not pin. Do not merge LPIN/IDWT.
Order: jobsite words → particle null → tokyo jp → matchKeptDesk → search-lite → layers → presets.
Dark comms still gets this local card. Fly opt-in on the card. Open desk is the drawer.
GO: India/Italy by name; tokyo→jp; jobsite words→hole. NO-GO: particle iso2; tokyo→nrt; jobsite without words.
Leftover for other seats: desks.ts still iso2-exact `in`/`it` on the command bar; InsightCard Fly lat/lon.
Hour 0 develop. Coordinator owns closeout. Not a ship.
