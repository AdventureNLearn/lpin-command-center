# HOUR 3 AUDIT — seat 08 `src/lib/kept/desks.ts`

**Class:** public-suite WIP · **not a ship**  
**Tree:** `sandbox/work/groks-eye-view-next`  
**Phase:** audit-heavy (CLOCK T+3)  
**Law:** `docs/session-chat-globe/USEFUL-TOOL.md`  
**Owns:** this note. No new features. `desks.ts` not edited (GO).

## GO / NO-GO (this file)

| Gate | Disk | Verdict |
|------|------|---------|
| Cap 33 | atlas `kit_on_disk: true` = **33**; join ∩ capital; `slice(0, 33)`. `_template` not an iso. Holes listed, not plotted. | **GO** |
| Seats filed only if `> 0` else 0 | `typeof filed === "number" && filed > 0 ? filed : 0`. Does **not** copy `named`. | **GO** |
| FR/KE/GH/ID/TR no fake full house | filed seats 0; named 595/348/275/569/591 stay named-only via `keptCountLine`. | **GO** |
| `keptCountLine` | named 0 → `no sourced roster`; seats 0 → `{n} named`; else `{n} of {s} named`. `keptContact` uses it. | **GO** |
| `in` / `it` particles | 2-letter branch returns **null**. `keptDeskByIso("in"\|"it")` still explicit. | **GO** |
| No invented sittings | `named` = `counts.members` ?? `sitting` ?? 0 as filed. No pad, no trim (US 437/435, KR 302/300). Empty cl/fj/nl/th/tz stay 0. | **GO** |

**Everyday:** empty kit is empty; unknown chamber is not a full house.  
**Serious:** delayed / sourced / hole. Counts as filed. No auto-truth.

## Execute

**Stop.** This file has no FAIL leftover. No `src/` patch this hour.

Out of seat: consumers already import `keptCountLine` (insight-local, LayerSubs, DeskDrawer). Not re-opened here.

## Verify

No `desks.ts` edit. `tsc --noEmit` **PASS**.

**Close:** seat 08 Hour 3 **GO**. Not a ship.
