# Cycle 1 — seat 13 `globeEngine.ts`

**Class:** public-suite WIP · **Not a ship**  
**Audiences:** everyday + serious (`USEFUL-TOOL.md`)  
**File owned:** `src/lib/intel/globeEngine.ts` only  
**Verdict:** **GO** — no jobsite pins. No `src/` edit.

## Iterate

Hour 0 (`hour0-13.md`) already: `grep jobsite` = 0. Subscribe loads flights…launches + LEG + AHJ. No `loadJobsite()`. Peek hover / `flyTo` camera primitive left alone.

## Audit

| Audience | Check | Disk |
|----------|--------|------|
| Everyday | Jobsite ask does not dump pins on the globe | **PASS** — no `jobsite` string, no `job-` entities |
| Everyday | “we don’t have that pack yet” stays a desk hole, not a map | **PASS** — layer on does not spawn billboards |
| Everyday | Look here / fly is optional | **PASS** — `flyTo` is a primitive; chat gate is not this file |
| Serious | 0 honest hole on the globe; no invented claims | **PASS** — extras only `eq-` `fire-` `msn-` `leg-` `ahj-` |
| Serious | No auto-zoom from this file | **PASS** — callers own `askedToMove` |
| Serious | Peek ≠ Insight | **PASS** — SAT `MOUSE_MOVE` only |

Subscribe 1451–1489: flights, military, vessels, satellites, earthquakes, fires, launches, legislatures, permits. **No jobsite.** First paint 1560–1566 same live feeds only. KIT-10 / 4h-14: do **not** add a branch.

## Forecast

Keep 0 pins. Next hourly cycle does **not** grow a jobsite entity loader. SAT hover FAIL (hour0-11 leftover, operator report) is smoke T+3:45 / PeekCard — not a pin dump. Empty-state desks and bubble copy are other seats (`FORECAST.md`).

## Execute

No patch. Engine already honest for both audiences: hole stays empty; globe does not lie with pins.

OPSEC: localhost harden. No municipality samples. Live gevradio frozen.
