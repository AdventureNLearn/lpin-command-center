# Hour 4 · seat 01 · insight-local.ts (POLISH)

**Class:** public-suite WIP · not a ship · 33 kits · no live gevradio · no Rest · no insind
**File:** `src/lib/intel/insight-local.ts` only. Law: `USEFUL-TOOL.md`. No new features.

## Iterate
Hour 3 audit GO. Named nits: intel-layer `source` always `Live intel · public feeds`; preset `source` `opt-in fly`.

## Audit
| Nit | Delayed / sourced honesty | Verdict |
|-----|---------------------------|---------|
| Live label on simulated vessels | `LAYER_META.vessels.freshness` is `simulated`. Holo-hint said Live. | **FAIL** |
| Preset `opt-in fly` | Not a live/delayed lie. Chrome still says Look here. | copy leftover, polish |

Gates from hour 3 (Japan/Canada/France card-not-yank, particles, no Rest, jobsite hole, ISS no auto-track) still **GO**.

## Forecast
Copy-only: bind layer `source` to `LAYER_META.source · freshness`. Preset hint → Look here optional. Do not touch permit Harbor string, ISS live source (station is a live contact), or logic gates.

## Execute
- `intelLayerInsight.source` = `${meta.source} · ${meta.freshness}` (vessels → Modeled shipping lanes · simulated).
- Preset `source` = `Grok comms · Look here optional`.
- `tsc --noEmit` green. Not a ship.
