# Hour 4 POLISH · seat 02 · `src/lib/intel/insight.ts`

**Class:** public-suite WIP · **Not a ship**  
**Tree:** `sandbox/work/groks-eye-view-next`  
**Law:** `docs/session-chat-globe/USEFUL-TOOL.md` · copy leftovers only · no new features  
**Hour 3:** `cycle-3-02.md` **GO**

## Copy on disk

User-facing strings in this file:

| String | Role |
|--------|------|
| *Named this place. Look here if you want the globe to follow.* | unasked-fly card body |
| `Grok comms · delayed` | default + fly-query source |
| `"Insight"` | blank-title fallback |

`askedToMove` still uses protocol verbs (`fly`, `take me`, `go there`, `show me on the globe`). `look here` is not in the regex. Comment on `insightFromFlyQuery` is not UI.

## Polish check

| Check | Verdict |
|-------|---------|
| Hour 3 GO still holds | **GO** |
| Look here copy, not Fly, on the unasked card | **GO** |
| Delayed source, no auto-truth fields | **GO** |
| Copy leftover in this file | **none** |

**Verdict: GO.** Stop.

## Execute

No edit to `src/lib/intel/insight.ts`. Did not run `tsc` (no edit). FORECAST.md not touched.
