# Hour 4 polish — seat 07 `runCommand.ts`

**Class:** public-suite WIP · **Not a ship**  
**Tree:** `sandbox/work/groks-eye-view-next`  
**Writer:** this note only. **No `src/` edit.**  
**Law:** `docs/session-chat-globe/USEFUL-TOOL.md`

## Confirm (Hour 3 still holds)

| Check | Disk | Verdict |
|-------|------|---------|
| No auto-zoom | `keptOpen` `flyTo` and `permitSearch` `lookupPlace` only if `askedToMove(raw)`. `open Canada` = desk + LEG, camera stays. Chamber fly-ask still flies. Explicit `{ type: "flyTo" }` (Look here / bar fly verbs) unchanged. | **GO** |
| Comms stay | Civic desks use `hushPicker()` (radio picker only). Corpus may `setOpen(false)`. | **GO** |
| No Rest dump | `searchAhj` only. No `searchRestAhj` / `setPermitRest`. | **GO** |

Everyday: open a 33-kit desk without a yank; Look here is optional.  
Serious: sourced desk, comms can stay, no auto-truth from this file.

## Stop

Polish: copy/Esc/card-vs-bar are other seats. No new features. No `tsc` (no edit).
