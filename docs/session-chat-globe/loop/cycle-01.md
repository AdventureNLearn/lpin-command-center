# Cycle 1 · seat 01 · insight-local.ts

**Class:** public-suite WIP · not a ship · 33 kits · no live gevradio · no Rest · no insind
**File:** `src/lib/intel/insight-local.ts` only. Everyday + serious (`USEFUL-TOOL.md`).
**AOS:** iterate → audit → forecast → execute

## Iterate
Hour 0 gates stay: particles `in`/`it` mint no India/Italy; comms `tokyo` is JP kept; jobsite only on jobsite words. `pickAhjCore` + ISS card already on disk. Card chrome (Look here / Open the file / Show on the map) is seat 03.

## Audit
Kept body named the chamber but not the capital, and treated 0 / vacancy / full roster as one “named” line. Jobsite said “Honest hole” (jargon) instead of “we don’t have that pack yet.” Permit missed “portal not verified” in the stranger sentence. ISS still said “Fly tracks.” Layers titled the raw id and said “Layer is available.” Preset still said Fly. Specialist evidence-gate (delayed / simulated / unverified / empty) was incomplete on the card.

## Forecast
Rewrite bodies only. Layer titles from `LAYER_META`. Jobsite title = typed locality. Logic gates unchanged. No auto-zoom. Peek ≠ Insight.

## Execute
- Kept: `{chamber} in {capital}`. Full / vacancy / empty roster lines. Delayed register, not a live roll. Look here optional. Open the file — empty means we do not have them.
- Jobsite: stranger hears we don’t have that pack; specialist gets empty list, 0 pins, no invented rows. Source stays `JOBSITE_HONESTY`.
- Permit: `ahjName` + catalog row + portal not verified + confirm with the local office. Core only.
- ISS: public tracking, not navigation, not classified. Look here follows. No lock-on unless asked.
- Layers: `LAYER_META.label` + source + freshness (vessels stay simulated). Show on the map optional.
- Preset: named place, not a desk. Look here optional.
- `tsc --noEmit` green.

## Leftover (not this file)
`desks.ts` still iso2-exact `in`/`it` on the command bar. `insight.ts` `insightFromFlyQuery` still “Fly if you want.” `comms.ts` layer body still the old one-liner.
