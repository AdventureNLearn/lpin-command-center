# Hour 0 DEVELOP — seat 06 `src/lib/feeds/chat.ts`

**Class:** public-suite WIP · **Not a ship**  
**Tree:** `sandbox/work/groks-eye-view-next`  
**File owned:** `src/lib/feeds/chat.ts` only (SYSTEM const)  
**Clock:** `loop/CLOCK.md` · hour 0 develop

## Act

1. **Prefer INSIGHT.** Tag block leads with Prefer INSIGHT. Normal questions stay card-only. One last tag line, not INSIGHT+ACTION. ACTION flyTo still only when they asked go/fly/take me/show me on the globe.
2. **No demo-city example.** Dropped canned `q:"Tokyo"` from ACTION. Both INSIGHT and ACTION examples use `q":"what they named"`. Do not seed q with a canned municipality.
3. **Honesty line.** Body is guidance, not a certified fact / score / city login. No invented claims, sittings, donors, names, or portals. Forbidden: verified, live portal, certified, I checked the login. Factory URLs unverified. Jobsite packs empty until typed. Incomplete is empty. Not a live claims desk. No skill brands. Human presses Fly / Open desk / Show layer.
4. **33-kit countries ok as q if the user named them.** iso2 or country name: `ar au br ca cl cn de eg es fj fr gb gh id il in it jp ke kr mx ng nl nz ph pl th tr tw tz ua us za`. Optional kept desk `{system:"kept",id:"iso2 they named"}` only then. Atlas holes: card without q and without desk.

Handler, rate limit, clipContext, model — unchanged.

## Not this seat

`comms.ts` parser gates, `insight.ts` `askedToMove`, InsightCard Fly, command bar, live gevradio, Rest dump, insind.

## Verify

`npm.cmd run typecheck` (`tsc --noEmit`) **PASS**.
