# KIT-19 — Command Center + Workdesk Generator

Filled from KIT-09. Named 2026-09-06 (D-268). **Ingest + template this sitting. Do not execute `src/` until the operator says execute KIT-19.**

```
FEATURE NAME: Command Center (context packs)
LAYER ID / KIND: pack registry, not a single layer. First packs: fifty / fifty, opencells / opencells
CONTACT MAPPING: none at orbit per pack unless that pack’s file says otherwise
MARKER BUDGET AT GLOBAL ZOOM: 0
DATA MODULE PATH: src/lib/packs/
LAZY? yes
DRAWER ADAPTER: one panel; pack file = kernel + open cells + typed lookup + honest hole
HONESTY STRIP: Packs are briefings/methods. Not legal advice. Not scores. Human final call. Empty is a finding.
COMMAND PHRASES: show fifty · hide fifty · open fifty desk · show opencells · hide opencells · open cells desk
FRESHNESS: delayed
PUBLIC COPY CONSTRAINTS: plain civic tooling; no skill brands; no muni samples; no iframe; no homepage swap
P0 KILL SWITCH: hide pack layer / close desk. Do not unmount Cesium. Do not steal radio.
```

## Execute later (when named)

1. Add `src/lib/packs/` registry. Load `fifty` and `opencells` lazy from the filled pack files.
2. Extend `InsightSystem` with `"pack"` **or** keep `intel` + `desk.system` union. One card chrome (`.holo-card`). No fourth UI.
3. `LayerId` += `fifty` | `opencells`. Default **off**. Exclusive rail (KIT-12).
4. Drawer adapters. Honest hole if the cell is open / not retrieved.
5. Commands as above. Look here only if `q` is a place.
6. Radio: pack stations extra. Creedence stays.
7. OPSEC gate. Smoke: globe orbits, radio plays, both packs open as cards/drawers, 0 orbit pins, no iframe.

## Do not this sitting

Patch `src/`. Remix gevradio. Fold LPIN. Recreate Kept 50. Dump city or country pins.
