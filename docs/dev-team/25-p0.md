# Seat 25 — P0 reconfirm (KIT-02 must not break)

**Class:** public-suite  
**Tree:** `REPO_ROOT`  
**Bots:** PARK  
**Corpus:** `vendor/kept/_out/` (read-only)  
**This sitting writes:** this file only  
**Not this sitting:** `src/` edits, Playwright, KIT-02 `DeskDrawer`, KIT-03+

Lock source: `KIT-00-SMOKE-LOCALHOST-2026-08-29` (obsidian gevradio-unification pack) + `GROK_BUILD_HANDOFF.md` P0s + `FEATURE_KITS.md` definition of done. Reconfirm is **disk**, not a new browser run.

---

## 1. KIT-00-SMOKE-LOCALHOST (the shell lock)

Logged 2026-08-29 against http://127.0.0.1:8080/ (`sandbox/work/groks-eye-view-next`). Civic code **was not started** that sitting. Seven intel layers only. No LEG/AHJ on the rail.

**5-line smoke (verbatim lock, do not weaken):**

1. Globe woke (Cesium ion credit, Earth visible). First-run four cards present. **Esc** closed the dialog. “Don't show this again” still there.
2. Styles **NVG3** then **Normal1**. Header after place command: **Tokyo**. Command bar still there at **390px** width.
3. **Play radio** → Pause + “Creedence Clearwater Revival - Someday Never Comes”. Comms opened (Tokyo · radio on) and **Close comms**.
4. FLT toggle on → **46 · adsb.lol** (no OpenSky key). Seven intel layers only. No LEG/AHJ.
5. `tokyo` (local parser) flew the place name. Hint `yo tokyo` did **not** fly without Grok parse — logged, not patched.

**Residuals (not a KIT-00 fail, not a KIT-02 job):** `cesiumStatic/*` 404 + `__grok/icon-180.png` 404; HUD lat/lon lagged while place said Tokyo; Windows `npm run dev` → `spawn vite ENOENT` (start via `node.exe scripts/with-app-env.mjs node.exe node_modules/vite/bin/vite.js …`); clone FLT source is **adsb.lol**, not live gevradio adsb.fi. Do not “correct” as a drive-by.

Shell files named in that smoke: IntelApp, GlobeCanvas, OverlayHud, RadioDeck, CommsChat, FirstRun, types, commands, radio, globeEngine.

Live gevradio KIT-00 is a **separate** log. D-251/D-254: this tree is the write target. Do not remix live gevradio.

---

## 2. OverlayHud / RadioDeck presence (disk reconfirm)

Still present. Still the cockpit chrome. KIT-02 may add a sibling panel; it must not replace these files or unmount them.

| Surface | Path | Presence |
| --- | --- | --- |
| Homepage | `src/routes/index.tsx` | `/` → `<IntelApp />` only |
| Shell | `src/components/intel/IntelApp.tsx` | `GlobeCanvas` + `DetectionOverlay` + `OverlayHud` |
| HUD | `src/components/intel/OverlayHud.tsx` | `export function OverlayHud` |
| Tuner | `src/components/intel/RadioDeck.tsx` | `export function RadioDeck` |
| Mount | OverlayHud ~L680 | `<CommsChat />` then `<RadioDeck />` then `<CorpusPanel />` |
| Globe boot | `src/components/intel/GlobeCanvas.tsx` | boot wrapper only; **no pin loop** |
| DeskDrawer | `src/components/desks/` | **absent** (KIT-02 not started) |

**Radio stays reachable from OverlayHud, not from a new shell:**

- Header Play/Pause toggles `useRadio.toggle()` (does not need the picker open).
- Header **Radio (R)** toggles `useRadio.setPicker`. Opening radio **closes** comms + layer rail + corpus.
- **G** comms closes radio picker + rail + corpus. **L** layers closes comms + radio picker + corpus.
- Esc stack: comms → radio picker → first-run → corpus → cockpit → track → layer rail.
- Footer command bar is a sibling (`footer` `z-30`), hidden only when `clean` or `chatOpen`. Radio picker does **not** eat it.
- `RadioDeck` always mounts. Picker UI is gated; `bindRadioPlayer` runs on mount. Unmounting `RadioDeck` kills audio even if the Play button remains.

**Parser lock (`src/lib/intel/commands.ts`):** radio presets + `/^(play |put on |spin )?(some )?(ccr|creedence)$/i` → `{ type: "radio", id: "ccr", on: true }` **before** layer on/off. Station aliases in `src/lib/intel/radio.ts`: `ccr`, `creedence`, `swamp rock`. Layer on-words are only `on|enable|show|turn on|light up`. Do not steal `put on` for desks.

**Honest drift since KIT-00 smoke (already on disk, not a P0 fail):** KIT-01 registered `legislatures` / `permits` on `LayerId`, `LAYER_META` (LEG / AHJ, delayed), `LAYER_ORDER` (nine rows), store defaults **off**. Corpus panel exists (`corpusOpen`, OverlayHud Corpus button, `show corpus`). globeEngine subscribe is still the **seven intel** loads only — no LEG/AHJ entity branch. First-run is still the original four cards. CommandAction has `corpus`; it does **not** yet have `{ type: "desk" }`.

---

## 3. P0s KIT-02 must not break

Fail any one → stop the kit and revert (rule in §4). These are FEATURE_KITS definition-of-done + KIT-00 smoke + handoff hard rules, applied to the empty DeskDrawer sitting.

1. **Globe still orbits.** Homepage is Cesium (`GlobeCanvas` → `bootGlobe`). Drag orbit, scroll zoom. Do not unmount the canvas. Do not add a civic route that replaces `/`. DeskDrawer is a panel over Earth, not a new site.
2. **OverlayHud and RadioDeck stay mounted.** `IntelApp` composition stays Globe + Detection + OverlayHud. OverlayHud still renders `<RadioDeck />`. Do not restyle the HUD. Do not fold the tuner into a civic desk.
3. **Creedence from the command bar.** `put on creedence` / Play still starts station `ccr`. Pause control survives. Tuner stays music. Do not merge scoring language into radio copy.
4. **Comms / first-run / styles 1–6 / command bar** stay reachable with a desk open or closed. First-run four cards stay: Planes, please / Space junk / The planet is yelling / Just the globe. Esc + “Don't show this again” stay. KIT-02 must not replace those cards.
5. **390px command bar usable.** KIT-02 mobile = bottom sheet that does **not** permanently cover the footer. No horizontal trap. Same lock as KIT-00 line 2.
6. **Seven intel layers unchanged.** FLT / MIL / AIS / SAT / EQ / FIR / MSN still default off, still draw only when toggled. FLT on may still report **adsb.lol**. LEG / AHJ stay default **off**; empty drawer is not a pin dump. globeEngine gets **no** civic subscribe this kit.
7. **No 16k pins.** Permit Rest on disk is **14925** (count, not a map). Capitals **194**. Do not import `src/lib/permit/places-rest.ts` (or current `places.ts`, which concatenates Rest) on first paint. Do not walk `_out/capitals.json` or harvest members from DeskDrawer. KIT-03/04/06 own drawing.
8. **No skill brands.** Public copy = plain civic tooling. No FROGNET / GOYNET / Layer-0 / swarm names / investigator legal names / invented sitting names. Radio stays music. LEG source string stays “Kept harvest / public registers”; AHJ source stays “Permit Harbor catalog (locked 2026-08-18)”.
9. **No iframe-as-integration.** Do not iframe keptglobal or hivepermitdev. Do not certify factory / Rest portal URLs. Honest incomplete > padded complete. Bilawal Sidhu credit and the public-data / not-for-navigation strip stay.
10. **Do not kill Remix / live gevradio / `_out`.** No `npm install`, no git push, no package.json churn, no write to `vendor/kept/_out/` or `vendor/kept/kits/`, no live gevradio remix.

KIT-00 residual (`yo tokyo` without Grok parse) is still not a KIT-02 job.

---

## 4. Fail-closed revert rule

Handoff §6: **If any P0 fails, roll back the civic layer, keep the globe.**

FEATURE_KITS: if a kit would violate a P0 (16k pins, radio dead, globe unmounted, invented names, certified-fake portals), **skip the kit and report the block**. Do not “finish the drawer anyway.”

**KIT-02 apply:**

- Before coding: confirm localhost still orbits + Creedence. If that P0 is already red, **do not start DeskDrawer**.
- During coding: globe dead / OverlayHud-only page / RadioDeck unmounted / Creedence stolen / command bar gone at 390px / Rest bundled or plotted / invented names / iframe “integration” → **revert the civic diff immediately**. Keep GlobeCanvas, OverlayHud, RadioDeck, FirstRun, intel layers.
- Civic BASE / empty “No desk selected” text is **not** worth a dead cockpit. Revert means delete `src/components/desks/**` and unwind store `desk` / `{ type: "desk" }` / HUD open-desk wiring. Do not leave a half-route.
- FPS death from a pin loop is a P0: the layer/drawer is wrong, not the globe. Raise nothing; **cut the loop** and revert.

Stop. Do not start KIT-03 in the same turn as a revert.

---

## 5. E / I / A

**E.** KIT-00-SMOKE-LOCALHOST 2026-08-29: globe, four first-run cards, NVG3/Normal1, Tokyo, 390px command bar, Creedence play/pause, comms open/close, FLT · adsb.lol, seven intel layers, no LEG/AHJ that sitting. Disk now: `IntelApp` still Globe + Detection + OverlayHud; OverlayHud still imports and mounts `RadioDeck`; Play/R/G/L/Corpus mutual-exclusion intact; Esc stack intact; `put on creedence` still radio `ccr` before layers; FirstRun still four cards; `LAYER_ORDER` nine ids with LEG/AHJ default off; globeEngine subscribe seven intel loads; GlobeCanvas has no entity loop; `src/components/desks/` absent; `places.ts` still static-imports Rest; `_out` 33/33 PARK, capitals 194, rest 14925.

**I.** KIT-02 is one empty right-side / bottom-sheet panel. The failure modes that kill P0s are: unmounting Cesium for a desk route, dropping `RadioDeck` so audio dies, covering the 390px command bar, or using the empty drawer as an excuse to load Rest / capitals / members.

**A.** Accept this list as the KIT-02 P0 gate. Fail closed: revert civic drawer, keep the globe. Do not implement DeskDrawer from this seat. Do not run Playwright. Park bots. One kit per turn.

**Stop.**
