# SEAT 30 — REFUSE LIST

Seat: 30 · band: Fence (26–30, 32) · job: refuse  
Coordinator: Host Grok Build · date: 2026-08-29  
Target class: public-suite  
Tree: `C:\AOS\ops\local-reason-bridge\sandbox\work\groks-eye-view-next`  
This file only. Did not edit `src/`, `_out/`, or `kits/`.

Law for this 32-seat team: write only `docs/dev-team/<NN>-*.md`. One writer per file. Next implement = **KIT-02 empty drawer**. One kit per turn.

---

## Sources (read, not rewritten)

| Source | What binds |
| --- | --- |
| `Active/Handoffs/gevradio-unification/BUILD-PLAN-2026-08-29.md` §Refuse | Remix/overwrite live hosts · 16,165 Rest pins at orbit · skill/swarm chrome · invent sitting names / certified factory URLs · load `places-rest.ts` or fat `votes.json` on first paint |
| `Active/Handoffs/gevradio-unification/FEATURE_KITS.md` P0s | Skip the kit and report if it would: dump 16k pins, kill radio, unmount the globe, invent names, or ship certified-fake portals |
| FEATURE_KITS definition of done | Globe still orbits · Creedence from the command bar · desktop + narrow width · honesty strips visible · no new skill-brand UI copy |
| `MANAGE-AND-GROK-BOT-2026-08-29.md` §Refuse | iframe keptglobal / hivepermitdev · votes.json harvest · Bot edits `src/` · mix LPIN |
| `docs/dev-team/README.md` | Seats 11–25 prep only. Do not implement KIT-03+. Fence seats do not patch `src/` |
| WORKING-DOCUMENT 2026-08-29 GEV pickup | Playwright MCP Edge profile abandoned this sitting · Xplor stays dead · operator browser = `explorer.exe http://127.0.0.1:8080/` |

FEATURE_KITS P0s are stop-the-kit, not “fix later.” If any P0 would fail, **do not ship the kit**.

---

## Numbered do-not (this team, this sitting)

### 1. Do not remix live

Do not Remix, overwrite, push, or treat as a write target:

- `https://gevradio.grok.me` (D-251 / D-254: this tree is a **new app**; live gevradio is frozen reference)
- `https://keptglobal.grok.me`
- `https://hivepermitdev.grok.me`
- `upstream` (`AdventureNLearn/groks-eye-view`)

Do not click Remix on the live host. Do not merge this clone into live gevradio. Do not “promote” localhost to grok.me this sitting. KIT-08 one-click is a later kit on a **new** host, not a live overwrite.

### 2. Do not dump 16k pins

Never plot the Permit Rest catalog at orbit (or at any zoom this sitting).

- Catalog lock 2026-08-18: Core **79** · Extra **462** · More **699** · Rest **14925** = **16,165** desks.
- `_out/permit-index.json`: `rest_is_count_not_map: true`. Rest is a **count card**, not a map.
- FEATURE_KITS / handoff P0: global zoom + permits on = **0 Rest-layer pins**.
- KIT-01 already registers AHJ default **off** with empty contacts. Do not subscribe `globeEngine` / `flatEngine` to LEG or AHJ.
- KIT-03 cap later is ≤ ~200 country markers (`capitals.json` count **194**). That is **not** this sitting and is still not Rest.
- KIT-06 is the only zoom gate for Rest, and even then: search hit / state pack / county-scale + clustering. Not orbit.

Any loop over `places-rest.ts`, `REST_PLACES`, or 16,165 `Contact`s is a P0 fail. Revert.

### 3. Do not iframe keptglobal

Do not iframe `keptglobal.grok.me` or `hivepermitdev.grok.me` as “integration.”

Civic work is a **layer + a drawer** in this cockpit. Homepage stays Cesium. KIT-02 is `src/components/desks/DeskDrawer.tsx` (empty kept/permit slots). An iframe of the live desks is not a desk adapter. Do not add a civic route that unmounts Earth.

### 4. Do not invent names

Do not invent sitting names, whip marks, donors, lobby clients, awards, fees, or AHJs.

- Names as filed, or a visible hole. Incomplete beats fake-complete.
- Do not pad claimed seats. Do not mint rosters for the **163** atlas rows with `kit_on_disk: false`.
- `cl` / `fj` / `nl` / `th` / `tz` sitting **0** stays 0.
- Do not call factory / Rest portal URLs verified (FEATURE_KITS P0: certified-fake portals).
- Public UI = plain civic tooling. No FROGNET / GOYNET / Layer-0 / swarm / skill brands / investigator legal names.

### 5. Do not rewrite `votes.json`

Do not write, harvest-into, truncate, or “complete” `vendor/kept/kits/**/votes.json` (or `members.json`).

- Gold kits: LastWriteTime **2026-08-20 / 2026-08-21**. Fence: no rewrite after 2026-08-22.
- Fat `votes.json` bodies are 30–87 MB (es/ua/us/br/pl/fr). Do not open them to invent rows. Do not load them into the HUD.
- `_out/packages/{iso}.json` is the index/holes strip. It does **not** stand in for fat votes.
- D-182 harvest shelf still binds. No `:8103`. No `populate-full`. No live keptglobal overwrite.
- This team’s writers stay in `docs/dev-team/`. Bots stay PARK on `_out/`. Neither rewrites kits.

Also refuse **first-paint load** of fat `votes.json` (BUILD-PLAN Refuse). KIT-02 empty drawer does not import it.

### 6. Do not import `places-rest` on first paint

Do not import or search `src/lib/permit/places-rest.ts` (~3.3 MB) on globe first paint.

- Do not import current `src/lib/permit/places.ts` either — it **static-imports** `REST_PLACES` and concatenates Rest into `PLACES` / `searchPlaces`. That is a KIT-04 split, not a “just this once” search.
- Smoke / KIT-02 must not pull Rest into the first-paint module graph.
- Integrity tests may parse Rest on disk; the HUD and globe must not.
- Rest stays the count on `permit-index.json` until KIT-06 opt-in (not this sitting).

### 7. Do not use Playwright MCP this sitting

Playwright MCP (Edge persistent profile) is **abandoned this sitting**.

- Failure mode already logged: tab steal / page-drop. Do not reconnect, heal, or re-run MCP smoke from these seats.
- Do not drive the operator’s Edge via `@playwright/mcp` to “verify” KIT-02 or corpus.
- Operator browser: `explorer.exe http://127.0.0.1:8080/`.
- Disk smoke that already exists (`docs/corpus-audit/smoke-corpus.mjs`, `tsc --noEmit`) is not an excuse to relaunch MCP.
- A connected Playwright MCP server in a Grok session is **not** a go. Seat 30: refuse the tool this sitting.

### 8. Do not restore Xplor

Xplor / Xplorer stays **dead**.

- `C:\xplor\Xplorer\Xplorer.exe` was missing while it still owned `UserChoiceLatest` for `http`/`https` → “Application not found.”
- Defaults were repaired to Edge. Reinstalling Xplor can re-hijack associations.
- Do not launch Xplor. Do not re-associate http/https to Xplorer. Do not use it as the KIT-00/KIT-02 smoke browser.
- Do not “fix” browser defaults from this team.

### 9. Do not write KIT-03+ code

Do not implement KIT-03, KIT-04, KIT-05, KIT-06, KIT-07, KIT-08, or KIT-09 from this 32-seat sitting.

| Kit | Forbidden this sitting |
| --- | --- |
| **KIT-03** | `src/lib/kept/**` · fat `members.json` in HUD · country `Contact`s · capital/chamber pins · KeptAdapter |
| **KIT-04** | AHJ typeahead · `places.ts` search before Rest split · PermitAdapter · fee tables |
| **KIT-05** | Civic grammar (`open korea`, `find AHJ in …`, `score the sitting…`) |
| **KIT-06** | Zoom-gated / clustered Rest pins · lazy Rest “just in case” |
| **KIT-07** | Streams / compare (max 8) |
| **KIT-08** | Remix / PWA / live-host one-click |
| **KIT-09** | Fourth system / add-a-feature |

Seats 11–25 may **prep** (docs only). They must not create those files.

KIT-02 implementers (07–10, 31) may specify empty `DeskDrawer` chrome + `desk: null`. They must not fill adapters, plot capitals, enable LEG/AHJ drawing, or load `packages/{iso}.json`.

This fence seat does not patch `src/` at all.

---

## FEATURE_KITS P0 — fail closed (any one = stop)

1. Globe unmounted / not orbiting / replaced by a table or iframe.
2. Radio dead (Creedence from the command bar must still play).
3. 16k Rest pins (or any Rest dump on first paint).
4. Invented sitting names (or padded rosters).
5. Certified-fake portals (factory URLs labelled verified).

Also stop: skill/swarm chrome in UI · honesty strip missing on civic data · command bar eaten at 390px · `npm install` / git push / `_out` mutation from these seats.

---

## E / I / A

**E — Evidence**

- BUILD-PLAN §Refuse (D-254): remix/overwrite gevradio · keptglobal · hivepermitdev; 16,165 Rest pins at orbit; skill/swarm chrome; invent sitting names or certified factory URLs; load `places-rest.ts` or fat `votes.json` on first paint.
- FEATURE_KITS: one kit per turn; skip if P0 (16k pins, radio dead, globe unmounted, invented names, certified-fake portals); done only if globe orbits, Creedence plays, honesty visible, no new skill-brand copy.
- KIT-02 file: “Do not navigate to a new route that destroys the globe. Do not iframe keptglobal.”
- KIT-04: do not import `places-rest.ts`; KIT-06 owns Rest opt-in.
- `ARCHIVE.md` / `_out/HANDOFF.md`: this tree is a new app; do not merge into live gevradio; do not edit `src/` or kits from Bot handoff.
- `docs/dev-team/README.md`: later-kit seats prep only; fence writes `docs/dev-team/` only.
- Fence 09: `votes.json` / `members.json` not rewritten after 2026-08-22; bots must not rewrite them.
- WORKING-DOCUMENT 2026-08-29: Playwright MCP Edge profile abandoned (tab steal / page-drop); Xplor stays dead; operator = Explorer → `http://127.0.0.1:8080/`; next kit = KIT-02 drawer only.
- `places.ts` still concatenates `REST_PLACES`. `src/components/desks/` still absent (KIT-02 create target).

**I — Inference**

- The cheapest way this team kills the cockpit is Remix-live, iframe-as-integration, a Rest pin loop, or MCP/Xplor browser thrash — not a missing adapter.
- Empty KIT-02 does not need votes, Rest, country markers, or Playwright. Those are how KIT-02 becomes KIT-03–06 in one turn.
- A connected Playwright MCP server is a temptation, not a mandate. The sitting already logged it unsafe.

**A — Assumption**

- Host implement of KIT-02 (later turn, named seats) will keep CorpusPanel, lazy 501, LEG/AHJ default off, and zero civic entities.
- Operators will not treat this refuse list as optional when a seat wants to “just smoke in MCP” or “preview keptglobal in an iframe.”
- Xplor will not be reinstalled this window.

---

## Stop

Nine refuses. FEATURE_KITS P0s fail closed. This team writes docs. Next code = **KIT-02 empty drawer only**. No live remix. No 16k pins. No iframe. No invented names. No `votes.json` rewrite. No Rest on first paint. No Playwright MCP this sitting. No Xplor. No KIT-03+ code.
