# SEAT 23 — 501 later-kit fence

Seat: 23 · band: later kits (prep only) · date: 2026-08-29  
Tree: `REPO_ROOT`  
Write: this file only. No `src/` edits. No `_out/` edits. No KIT-02+ implement.

**Claim:** WAVE-501 is a US public-filing **count** on disk. The corpus panel already prints those counts. KIT-02 must not show the graph. Co-occurrence is not a quid.

---

## E — Evidence

### HANDOFF / STATUS claims

`vendor/kept/_out/HANDOFF.md` (retrieved 2026-08-29):

| Field | Claim |
| --- | --- |
| file | `501-links.json` |
| iso2 | us |
| nodes | 199 |
| edges | 215 (fec 186, usaspending 29) |
| holes | 17 |
| bytes | 125973 |
| wave | WAVE-501 US AUDIT PASS |

`vendor/kept/_out/STATUS.md`: PARK. ACK WAVE-501 US: PASS nodes 199 edges 215 filings fec 186 usaspending 29 holes 17.

### Disk parse (this sitting)

`JSON.parse` of `vendor/kept/_out/501-links.json`:

| Field | Parse | HANDOFF |
| --- | ---: | --- |
| bytes | 125973 | match |
| iso2 | us | match |
| retrieved | 2026-08-29 | match |
| top-level keys | `iso2`, `retrieved`, `nodes`, `edges`, `holes` | match |
| nodes | 199 | match |
| edges | 215 | match |
| holes | 17 | match |
| `rel=contributed` | 186 | = fec claim |
| `rel=granted` | 29 | = usaspending claim |
| `filing=fec` | 186 | same 186 |
| `filing=usaspending` | 29 | same 29 |
| rel/filing mismatch | 0 | — |

Kinds (counts only): committee 166, org 33.

Score keys: `influence` 0, `score` 0, `quid` 0, `pagerank` 0, `centrality` 0. No rank/score field on nodes or edges.

### `load501Summary` (already wired)

`src/lib/intel/corpus.ts` lazy-`import()`s `501-links.json` inside `load501Summary()`. Not a top-level import. Return type is counts + hole `what` only:

- `nodes` = `nodes.length`
- `edges` = `edges.length`
- `fec` = count of `rel === "contributed"`
- `usaspending` = count of `rel === "granted"`
- `holes` / `holeWhat` = trimmed `holes[].what`

Does not return node names, ids, edges, amounts, or a layout.

That split matches HANDOFF (186 / 29) because every `contributed` edge is `filing=fec` and every `granted` edge is `filing=usaspending`.

### Corpus panel already shows counts

`src/components/intel/CorpusPanel.tsx` (HUD overlay, not a desk):

- Lazy-loads 501 on panel open.
- Prints `{nodes} nodes · {edges} edges ({fec} fec / {usaspending} spending) · {holes} holes`.
- Copy: “Public filings. Not an influence score.”
- `<details>` lists `holeWhat` only.

No graph. No node-name dump. KIT-01 corpus smoke already covers S7/S8 from `docs/corpus-audit/15-pickup-plan.md`.

### KIT-02 is not this surface

KIT-02 contract (`docs/corpus-audit/13-drawer-contract.md`, `docs/dev-team/README.md`): empty `DeskDrawer` — one panel, two adapters (Kept / Permit). Store `{ desk: null | { system, id } }`.

On disk now: `src/components/desks/DeskDrawer.tsx` exists and is mounted from OverlayHud. Corpus panel stays a separate HUD overlay. DeskDrawer still must not import 501-links.json or draw a graph. (Stale no-desks claim corrected 2026-08-29.)

### Hole `what` (17)

1. 990 Part VII officer names
2. FEC Form 1 treasurer names as person nodes
3. 990 Schedule I org-to-org grants
4. 990-PF Schedule B
5. Form 8872
6. FEC ENTITY_TP IND itemized persons
7. DCCC and NRCC 24K recipients beyond top 20
8. PAC 24K recipients beyond top 5 per committee
9. 501(c)(3) public-charity Schedule B names
10. 501(c)(4)/(c)(5)/(c)(6) donor names
11. member_id on FEC candidate committees
12. edge.program from 990 Schedule I purpose
13. edge.program_area preferred tags
14. edge.issue_id kit join
15. USAspending awarded/granted edges
16. USAspending member_id when award CD is missing or 90/98
17. program_area on 18 USAspending edges

Sit-history inside hole extras (not a second live count): hole 13 `edges_with_program_area: 0` is stale vs live parse; hole 15 `program_area_counts` is pre-block; hole 17 `dropped: 18` / `kept: 9` is the live tag set. Dashboard copy must use hole 17 + edge parse, not hole 13/15 counts.

---

## I — Inference

HANDOFF WAVE-501 PASS is true for this file’s counts. The file is two public-filing families in one JSON (FEC 24K `contributed` 186, USAspending `granted` 29), plus 17 honest holes. It is not an influence product.

**Co-occurrence is not a quid.** A PAC 24K row sitting next to an award permalink is two filings in one archive. `rel` is a filing verb, not capture. `member_id` is a roster join, not a vote or earmark. `program_area` / `issue_id` are sparse tags, not motive.

The HUD already has the only 501 surface this tree needs for later kits: counts + hole `what`. KIT-02 is an empty kept/permit drawer. Putting a 501 network, force layout, ranked list, or node-name dump in DeskDrawer would:

- treat smoke corpus as the desk
- skip the empty-drawer contract
- invite scoring / quid copy
- leak names the loader was written to omit

Later kits (KIT-03+) still do not implement a 501 graph. If a later sitting lists filings at all, it reuses `load501Summary` counts + `holeWhat`. It does not draw edges.

---

## A — Action

Prep only. This seat writes no code.

**KIT-02 (next implement, other seats):** empty DeskDrawer. Do **not** import `501-links.json`. Do **not** call `load501Summary`. Do **not** render nodes, edges, amounts, or a graph. Footer honesty is kept/permit holes, not a filing network.

**Later kits:** leave 501 on the corpus panel. Do not add influence/score/quid/pagerank copy. Do not merge FEC and USAspending into one money graph. Do not invent the 17 omitted classes. Print hole `what`, never the missing people.

**Host fence:** do not edit `src/` or `_out/` from this file. WAVE-501 stays PARK on disk.

### Stop

Counts match. Panel already shows them. KIT-02 must not show the graph. Co-occurrence is not a quid.
